# cc65 linker config for TMS_Mandel as a DevBench CodeTank program.
#
# Square tables are still built at boot in low RAM ($0C00-$0DFF).

MEMORY {
    ZP:    start = $0000, size = $0050, type = rw, define = yes;
    CODE:  start = $4000, size = $1000, type = ro, define = yes, file = %O;
}

SEGMENTS {
    ZEROPAGE: load = ZP,   type = zp;
    CODE:     load = CODE, type = ro;
}
