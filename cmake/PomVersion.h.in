#pragma once
// GENERATED by CMake (configure_file) from cmake/PomVersion.h.in, filled from the
// repo-root VERSION file — the SINGLE SOURCE OF TRUTH for the POM1 version.
// Do NOT edit this template's value or hardcode the version anywhere else in C++.
// To bump: edit VERSION, then run tools/set_version.sh to sync the static docs
// (README, shell.html, packaging READMEs). CMake + the packaging scripts read
// VERSION directly. A mismatch is caught by tools/check_version_sync.sh (ctest
// `version_sync`).
#define POM1_VERSION_STRING "@POM1_VERSION@"
