# TODO6502.md

Open work on the **6502 software side** (what runs *inside* the emulated Apple 1). Emulator-side work → `../TODO.md`.

**Tags** `[effort · impact]` — same scale as `../TODO.md`.

---

## 🧹 Project hygiene

- [ ] **Resolve `TODO` placeholders in `dev/projects/*/README.md`** `[S · solid]` — 29 / 31 READMEs still ship literal `TODO` markers in three slots: *Recommended POM1 preset*, *POM1 → Presets → … (TODO)*, *License: TODO*. Pin each project to a canonical preset (text → preset 0/1, GEN2 HGR → preset 7, TMS9918 → preset 2 or 8, GT-6144 → its dedicated preset, A1-IO & RTC → its preset, CodeTank → preset 2) and pick a license. Already clean: `a1io_rtc_clock`, `lib_smoke`.
- [ ] **Per-project `LICENSE` headers in `.asm`** `[S · nice]` — once licenses are picked above, add a 3-line comment header so a copy-pasted file carries its license. Most files already have an attribution line (`VERHILLE Arnaud - 2026`); just append the SPDX identifier.

---

## 🆕 Candidate new programs

- [ ] **Applesoft Lite arcade pack** `[S · solid]` — leverage `GET A$` (no-Enter input) for 1977-style scroll-text arcade games: *Hammurabi*, *Lunar Lander*, *Star Trek* shoot-em-up. Pure scroll-text (no `HOME`/`VTAB`) — design for it. Save as `sdcard/<NAME>#F80801`.
- [ ] **TMS9918 Mode 2 (bitmap) demo** `[M · solid]` — `dev/lib/tms9918/tms9918m2.asm` driver ships but no project consumes it. Build a showcase (Mandelbrot in 256×192 colour, microSD image viewer, vector demo) to validate the driver and seed the next wave of TMS games.
- [ ] **HGR sub-byte sprite showcases** `[S · solid]` — `dev/lib/hgr/subbyte4.inc` + `subbyte_fill.asm` shipped with the library reorg, but only `hgr_maze` exercises sub-byte masks. Pin the lib with one or two dedicated example projects (arbitrary-width sprite blitter, scrolling text on HGR).
- [ ] **microSD shell utilities** `[S · nice]` — small Wozmon-callable tools in 6502 asm: `CAT` (dump file via `$D012`), `XCOPY` (range copy with progress), `HEXDUMP`. Ship under `sdcard/UTILS/` with tagged filenames.
- [ ] **More CodeTank ROM bundles** `[S · nice]` — `roms/codetank/Codetank_GAME1.rom` ships a 6-game compilation (Galaga/Sokoban/Snake/Life lower jumper, Tetris/LOGO upper jumper). Build themed companions: `Codetank_DEMOS.rom` (graphics demos), `Codetank_BASIC.rom` (BASIC + tools), `Codetank_PUZZLES.rom` (Connect4/Maze3D/Sokoban variants). Drop under `roms/codetank/` for the in-app library to discover.
- [ ] **A1-IO & RTC sensor logger** `[S · nice]` — extend `a1io_rtc_clock` from clock display to a logger that samples the DS3231 temp + 8 ADC channels + 4 digital inputs every N seconds and prints a CSV row.
- [ ] **GT-6144 Game of Life cycle-count writeup** `[S · nice]` — `gt6144_life` already runs; what's missing is the comparison vs `hgr10_life`. The 2102 SRAM is write-only via `$D00A`, so each generation needs a host-side shadow — surface that trade-off in both READMEs.

---

## 🔄 WIP cleanup

- [ ] **`tms9918_galaga`** — 4400-line `.asm`, three linker variants (stock cassette, CodeTank lower, CodeTank bank). Confirm playability across all three, capture a screenshot for `README.md`, and document the sprite-pattern layout (the embedded ASCII-art pixel maps are extensive but undocumented at the top level).
- [ ] **`tms9918_orbital_pool` / `hgr_orbital_pool`** — confirm both ports converge on the same physics (or document why they intentionally differ). Cross-link the two READMEs.
- [ ] **`tms9918_tetris_loader`** — name suggests a loader that pulls Tetris from elsewhere (CodeTank? microSD?). Document the two-stage flow at the top of the `.asm` so readers don't expect Tetris itself in this project.
- [ ] **`tms9918_codetank_menu` / `tms9918_codetank_menu_upper`** — clarify the relationship (lower- vs upper-jumper variant of the same menu?) and whether the duplication is justified or one config can drive both.

---

## 📚 Documentation

- [ ] **`dev/Programming_Apple1_ASM.md` — TMS9918 Mode 2 chapter** `[S · nice]` — currently covers Graphics I (Mode 1) only. Add a Mode 2 section once the demo above ships, mirroring §6 layout (VRAM tables, init sequence, sprite handling, multi-colour trick).
- [ ] **`dev/APPLE1DEV.md` — sub-byte HGR mini-tutorial** `[S · nice]` — currently a one-paragraph forward to `Programming_Apple1_ASM.md` §5. Now that `dev/lib/hgr/subbyte4.inc` lands, demote to a "use the lib" recipe with a 10-line example.
- [ ] **Per-project README screenshots** `[S · nice]` — bundle `dev/projects/<name>/screenshot.png` (text → terminal screenshot, HGR/TMS/GT-6144 → graphics window) so the in-app Source Browser can preview them later.

---

## 🛠️ Tooling

- [ ] **Per-project `make test` target** `[S · nice]` — most Makefiles already build `.bin` + `.txt`; add an optional `make test` that drives POM1 via the telnet harness (`tools/test_*_telnet.py`) and asserts a known on-screen marker. Smoke-test for "did this project still load?" once the GitHub Actions matrix from `../TODO.md` lands.
