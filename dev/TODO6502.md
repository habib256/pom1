# TODO6502.md

*[← dev/ index](README.md)* · libraries: [`lib/`](lib/README.md) · build layer: [`cc65/`](cc65/README.md)

Open work on the **6502 software side** (what runs *inside* the emulated Apple 1). Emulator-side work and shipped items are tracked in POM1's top-level TODO and changelog.

**Related:** the library hub [`lib/README.md`](lib/README.md) (asm + C tracks, the ZP contract, build integration); per-card programming tutorials ship with POM1's user documentation; CLI flags are documented there too.

**Tags** `[effort · impact]` — same effort/impact scale used by the emulator-side TODO.

> **Open work only.** Shipped items move to POM1's changelog. Last sweep **2026-07-12**: (1) the juillet CodeTank reorg shipped the 4 themed cartridges (CLASSICS / BASIC_LOGO / ARCADE / DEMOS) — that item left this list; (2) Rogue and Maze3D are ported to the GEN2 HGR card (`sketchs/gen2/game_{rogue,maze3d}`), and the byte-aligned HGR building blocks they factored out now live in `dev/lib/gen2/` (`hgr_sprite16.asm`, `hgr_text8.asm`, `hgr_blit2.asm`, `rev7.inc`) with micro-tests t14/t15 — the remaining private copies to converge are listed below. Previous sweep 2026-06-20: GEN2↔TMS9918 graphics-lib factoring done end-to-end (axes 1-3); only tail items remain.

---

## 🧹 Project hygiene

- [ ] **Per-project licensing (SPDX)** `[S · nice]` — no `.asm`/`.c` under `dev/` carries an `SPDX-License-Identifier`. Most files already have a `VERHILLE Arnaud - 2026` attribution line; pick a license per project and add a 3-line SPDX header so a copy-pasted file keeps its license.

---

## 🆕 Candidate new programs

- [ ] **BASIC arcade pack** `[S · solid]` — leverage `GET A$` (no-Enter input) for 1977-style scroll-text games (*Hammurabi*, *Lunar Lander*, *Star Trek*). Pure scroll-text, no `HOME`/`VTAB`. No source projects exist yet (the `sdcard/PLAB/ASOFT/` payloads are binary-only). First step: one game under `sketchs/apple1/asoft_<name>/` with a `.bas` source + tagged-binary build (`sdcard/<NAME>#F80801`).
- [ ] **HGR sub-byte sprite showcases** `[S · solid]` — `dev/lib/gen2/subbyte4.inc` (`sb4_byte_off`/`sb4_mask1`/`sb4_mask2`) + `subbyte_fill.asm` (`subbyte_fill_4`) ship with no consumer. Pin them with a dedicated example: an arbitrary-width sprite blitter, or pixel-granular HGR scrolling text.
- [ ] **microSD shell utilities** `[S · nice]` — small Wozmon-callable asm tools: `CAT` (dump file via `$D012`), `XCOPY` (range copy w/ progress), `HEXDUMP`. `sdcard/PLAB/UTILS/` ships tagged binaries with no source. Ship under `sketchs/apple1/sd_utils_*/` with a `Makefile` + `dev/cc65/` config.
- [ ] **A1-IO & RTC sensor logger** `[S · nice]` — the read path is extracted to `dev/lib/a1io/a1io.asm` (`a1io_read_reg`: X=reg 0..23 → A). What remains is the logger: sample the DS3231 temp + 8 ADC channels + 4 digital inputs every N s and print a CSV row. Build `sketchs/apple1/a1io_logger/` on the existing helper.
- [ ] **GT-6144 Game of Life cycle-count writeup** `[S · nice]` — `gt6144_life` runs; what's missing is the comparison vs `hgr_life`. The 2102 SRAM is write-only via `$D00A`, so each generation needs a host-side shadow buffer — surface that trade-off in both READMEs.

### Shipped lib, one consumer — needs a showcase

- [ ] **SID music player + tunes** `[M · solid]` — the SID lib is single-voice (`sid_play.asm` + `sid_notes.inc`), used only by `sketchs/apple1/sid_piano`. (1) add a 3-voice helper (`sid_v2_*`/`sid_v3_*`) to `dev/lib/sid/`; (2) build a music-player + 1-2 jingles under `sketchs/apple1/sid_music_*/`, tagged for `software/SOUND SID/`.
- [ ] **GEN2 colour showcase** `[S · solid]` — the NTSC-artifact colour pass (`gen2_hgr_puts_color`/`gen2_colorize_asm`: VIOLET/GREEN/ORANGE/BLUE) has one consumer (`GEN2Snake.c`). Build a dedicated colour demo under `sketchs/gen2/` (colour bars / colourised title / a colour variant of `demo_countdown`) to exercise the four carriers + `hibit` and give the Bench's GEN2-C target a colour example.
- [ ] **HGR bestiary — 2nd browser or split-bank** `[S · nice]` — `sketchs/gen2/demo_bestiary` pages 6 of 15 sprite categories (54 sprites) via the byte-aligned STA blit. The 8 KB dual-bank can't hold all 15 (11.3 KB) at once; add a 2nd curated browser (swap the `cat_*` tables) or a split-bank cfg for the rest. NB: the 48 B/sprite bank is **not** `gen2_hgr_blit`-compatible — the asm STA blit is the right path. Consider serving the pages straight from the TMS-format patterns via `dev/lib/gen2/hgr_sprite16.asm` (`hgr_spr16_x2` + `HSPR_*` colour) instead of the private `dblL`/`dblR` doubler — that also fixes the doubler's dropped-column seams.

### Converge the remaining private HGR text/blit copies

- [ ] **`GEN2_Chess` `putc_hgr` → `hgr_text8`** `[S · nice]` — chess carries the last private byte-aligned glyph emitter (`putc_hgr`/`puts_hgr` over `bbfont_ascii5f`). `dev/lib/gen2/hgr_text8.asm` covers it (HGR bit order, `ht_wrap = 40` — the chess panel writes past column 35). Rogue and Maze3D already migrated; hashes must stay pixel-identical (see their headless smoke recipes).
- [ ] **`game_sokoban` `draw_tile` inner loop → `hgr_blit2`** `[S · nice]` — the 14×16 tile copy is `hgr_blit2` in STORE mode. Watch the dual-bank CODE budget ($E000, 4 KB): the module adds ~180 B but removes the inline loop.

---

## ⚡ gen2c — perf (profiled 2026-06-16)

> PC-histogram profiling of GEN2 HGR Snake during gameplay. Aside from the deliberate `throttle` (32%), the cost is cc65 call glue + arithmetic helpers (~31%) spread across the per-frame path. `gen2_pixrect_asm` (13%) is already asm and near-optimal — **don't touch it**. Two real levers:

- [ ] **Dedicated asm cell blitter `gen2_hgr_cell(cx, cy, set)`** `[S · solid]` — one asm call taking coords **in cells**: does the `cx*8`/`cy*8` (shift) and the 6×6 fill internally, removing the C wrappers `draw_cell`/`erase_cell` + the `jsr aslax3` per cell. Biggest gain on the cc65-glue 31%. Add to `dev/lib/gen2c` alongside `gen2_pixrect_asm`; Snake wires `draw_cell`/`erase_cell` onto it.
- [ ] **Snake body as ring-buffer** `[S · solid]` — algorithmic: the `for(i=slen-1;i>0;--i)` shift in `tick()` is O(length) every tick. Replace with circular head/tail indices → O(1). Touches `tick()` + `draw_cell` loop + collision in `GEN2Snake.c` only.

---

## 🧩 GEN2 HGR ↔ TMS9918 graphics-lib factoring — tail items

> Axes 1-3 landed (see CHANGELOG). Only these remain:

- [ ] **Pixel-exact gfx regression** `[S · solid]` — the rewired sources all **build** green, but no golden image exercises a `gfx_*` forward (the frozen `hgr_testcard`/TMS testcard goldens predate the rewiring). Add a `tools/test_gfx_regress.py` golden on a GEN2 project + a TMS bitmap demo that actually routes through `gfx_line`/`gfx_circle`/`gfx_text`.
- [ ] **TMS `gfx_text` render check** `[S · nice]` — the axis-3 façade's GEN2 variant is render-verified; the TMS variant links + draws via `screen2_putc` but its visual wasn't captured headless (CodeTank ROM banking + Wozmon auto-run). Confirm `sketchs/portable/hello_gfx_text/`'s TMS build in the interactive Bench (or fold it into the regression golden above).
- [ ] ~~Single-source the Wozmon shim~~ **resolved as a drift gate (2026-07-12)** — a physical merge was declined: the DevBench copies sources by basename (relative `.include` breaks in-Bench builds) and `tms9918c/apple1_asm.s` is pinned by `dev/bench/tms9918c.json`, `src/Pom1BenchHost.cpp` and `tools/build_codetank_rom.py`. The Wozmon entry was already unified on `$FF1A` (June 2026); the remaining duplication is now asserted instruction-identical by `tools/check_wozmon_shims.py` in `make -C dev/lib check`. Reopen only if the Bench ever learns path-preserving source sets.
- [ ] *(optional)* **BB `FONT[768]` for TMS bitmap text** `[S · nice]` — emit a Beautiful Boot `FONT[768]` from `tools/build_shared_font.py` to also switch TMS bitmap text (`c64font.c`), so both cards share one font everywhere. (GEN2 8×8 façade cells stay white — NTSC colour lives on the 16×16 glyphs.)

---

## 🔄 WIP cleanup

- [ ] **`sketchs/tms9918/game_galaga` screenshot + 4-variant playability** `[S · nice]` — confirm playability across the four linker variants (stock `$0280`, CodeTank split `$4000`, menu-bank `$4100`, dualslot8k `$4000`) and capture a `screenshot.png` (none yet). The dualslot8k variant is canonical for **Silicon Strict** (see the TMS9918 programming tutorial's silicon-strict chapter in POM1's user docs).
- [ ] **Orbital Pool TMS9918 ↔ GEN2 HGR physics convergence** `[M · nice]` — both ports shared level data, controls, a 16-direction LUT and a 1..7 power scale with distinct `apply_gravity` paths. Neither lives in-tree now. If revived, either converge the two `apply_gravity` paths onto a shared `lib/m6502/`-style helper or document why each is tuned for its raster.

---

## 📚 Documentation

- [ ] **TMS9918 Mode 2 chapter for the Apple-1 ASM programming guide** `[S · nice]` — the Mode 2 demos ship (`tms9918_mandel`/`asteroids`/`maze3d`/`light_corridor`), so the guide (Graphics I only today) can document Graphics II: VRAM tables, `init_vdp_g2`, sprite handling, the multi-colour trick. (The guide ships with POM1's user docs.)
- [ ] **Sub-byte HGR mini-tutorial for the dev workflow guide** `[S · nice]` — its HGR section forwards to the ASM programming guide's sprite chapter. Add a 10-line `subbyte4.inc` recipe once the sub-byte showcase project (above) exists.
- [ ] **Per-project README screenshots** `[S · nice]` — bundle `sketchs/<profile>/<name>/screenshot.png` (text → terminal capture, HGR/TMS/GT-6144 → graphics window) so the in-app Source Browser can preview them. None exist yet. Wire to TerminalCard's `Ctrl-S` capture (`screenshots/pom1_latest.png`).

---

## 🛠️ Tooling

- [ ] **Wire `make test` bodies** `[M · solid]` — `dev/cc65/Makefile.common`'s `test` target is a no-op stub. Projects opt in via `TEST_CMD` = a `tools/test_*_telnet.py` invocation asserting an on-screen marker after `make all`. **`hgr_sokoban` is the pilot** (only project with `TEST_CMD`). Extend to the text-mode games (Connect4, Sokoban, Little Tower); the telnet harness already exists. The emulator-side TODO's CI matrix consumes this.

