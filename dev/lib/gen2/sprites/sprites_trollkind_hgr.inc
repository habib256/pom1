; ============================================================================
; sprites_trollkind_hgr.inc  --  constants for the TROLLKIND HGR sprite library
; ----------------------------------------------------------------------------
; Auto-generated by tools/build_hgr_sprites.py. Project rule: `.include`
; this header to get the count constants in immediate-mode form
; (e.g. `LDA #TROLLKIND_HGR_COUNT`), then `.import` the data labels from the
; sister sprites_trollkind_hgr.asm linked in via EXTRA_ASM. Same
; convention applies to every other category in dev/lib/gen2/sprites/.
; ============================================================================

TROLLKIND_HGR_COUNT             = 4
TROLLKIND_HGR_BYTES_PER_SPRITE  = 48
TROLLKIND_HGR_W                 = 3
TROLLKIND_HGR_H                 = 16
