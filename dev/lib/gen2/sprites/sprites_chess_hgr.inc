; ============================================================================
; sprites_chess_hgr.inc  --  constants for the CHESS HGR sprite library
; ----------------------------------------------------------------------------
; Auto-generated by tools/build_hgr_sprites.py. Project rule: `.include`
; this header to get the count constants in immediate-mode form
; (e.g. `LDA #CHESS_HGR_COUNT`), then `.import` the data labels from the
; sister sprites_chess_hgr.asm linked in via EXTRA_ASM. Same
; convention applies to every other category in dev/lib/gen2/sprites/.
; ============================================================================

CHESS_HGR_COUNT             = 6
CHESS_HGR_BYTES_PER_SPRITE  = 48
CHESS_HGR_W                 = 3
CHESS_HGR_H                 = 16
