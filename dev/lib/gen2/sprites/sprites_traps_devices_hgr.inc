; ============================================================================
; sprites_traps_devices_hgr.inc  --  constants for the TRAPS_DEVICES HGR sprite library
; ----------------------------------------------------------------------------
; Auto-generated by tools/build_hgr_sprites.py. Project rule: `.include`
; this header to get the count constants in immediate-mode form
; (e.g. `LDA #TRAPS_DEVICES_HGR_COUNT`), then `.import` the data labels from the
; sister sprites_traps_devices_hgr.asm linked in via EXTRA_ASM. Same
; convention applies to every other category in dev/lib/gen2/sprites/.
; ============================================================================

TRAPS_DEVICES_HGR_COUNT             = 12
TRAPS_DEVICES_HGR_BYTES_PER_SPRITE  = 48
