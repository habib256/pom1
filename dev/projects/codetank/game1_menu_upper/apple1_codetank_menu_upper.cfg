# cc65 linker config for the upper-bank dispatcher menu.
# 128-byte slot at CPU $4000 (CodeTank upper bank), ahead of the Tetris
# payload at $4080.

MEMORY {
    CODE:  start = $4000, size = $0080, type = ro, define = yes, file = %O;
}

SEGMENTS {
    CODE:  load = CODE, type = ro;
}
