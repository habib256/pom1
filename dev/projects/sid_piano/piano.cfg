MEMORY {
    CODE:   start = $0600, size = $0250, fill = yes, file = %O;
    SCREEN: start = $0850, size = $0200, fill = yes, file = %O;
}

SEGMENTS {
    CODE:   load = CODE,   type = ro;
    SCREEN: load = SCREEN, type = ro;
}
